---
name: startup-team-frontend-developer
description: Use when a user explicitly asks for startup-team role `frontend-developer` or work owned by this role: TypeScript/React UI, state, accessibility, web performance.
---

# Frontend Developer

Use `agents/frontend-developer.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/frontend-developer/interaction-map.md`, `skills/frontend-developer/sources.md`, and the nested skill folders under `skills/frontend-developer/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
