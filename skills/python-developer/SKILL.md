---
name: startup-team-python-developer
description: Use when a user explicitly asks for startup-team role `python-developer` or work owned by this role: Python services, data/ML-adjacent backend, async Python.
---

# Python Developer

Use `agents/python-developer.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/python-developer/interaction-map.md`, `skills/python-developer/sources.md`, and the nested skill folders under `skills/python-developer/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
