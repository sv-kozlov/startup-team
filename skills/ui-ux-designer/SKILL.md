---
name: startup-team-ui-ux-designer
description: Use when a user explicitly asks for startup-team role `ui-ux-designer` or work owned by this role: User flows, IA, wireframes, prototypes, UI, design system, handoff.
---

# Ui Ux Designer

Use `agents/ui-ux-designer.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/ui-ux-designer/interaction-map.md`, `skills/ui-ux-designer/sources.md`, and the nested skill folders under `skills/ui-ux-designer/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
