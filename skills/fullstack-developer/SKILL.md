---
name: startup-team-fullstack-developer
description: Use when a user explicitly asks for startup-team role `fullstack-developer` or work owned by this role: End-to-end feature delivery across the web stack.
---

# Fullstack Developer

Use `agents/fullstack-developer.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/fullstack-developer/interaction-map.md`, `skills/fullstack-developer/sources.md`, and the nested skill folders under `skills/fullstack-developer/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
