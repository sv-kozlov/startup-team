---
name: startup-team-product-manager
description: Use when a user explicitly asks for startup-team role `product-manager` or work owned by this role: Strategy, discovery, roadmap, product outcomes, north-star metrics.
---

# Product Manager

Use `agents/product-manager.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/product-manager/interaction-map.md`, `skills/product-manager/sources.md`, and the nested skill folders under `skills/product-manager/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
