---
name: startup-team-backend-go-developer
description: Use when a user explicitly asks for startup-team role `backend-go-developer` or work owned by this role: Go services, concurrency, error handling, data layer in Go.
---

# Backend Go Developer

Use `agents/backend-go-developer.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/backend-go-developer/interaction-map.md`, `skills/backend-go-developer/sources.md`, and the nested skill folders under `skills/backend-go-developer/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
