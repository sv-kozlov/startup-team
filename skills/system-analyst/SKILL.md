---
name: startup-team-system-analyst
description: Use when a user explicitly asks for startup-team role `system-analyst` or work owned by this role: Requirements, specifications, API/integration contracts, data contracts.
---

# System Analyst

Use `agents/system-analyst.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/system-analyst/interaction-map.md`, `skills/system-analyst/sources.md`, and the nested skill folders under `skills/system-analyst/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
