---
name: startup-team-mobile-developer
description: Use when a user explicitly asks for startup-team role `mobile-developer` or work owned by this role: iOS / Android / cross-platform apps, mobile UX, store delivery.
---

# Mobile Developer

Use `agents/mobile-developer.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/mobile-developer/interaction-map.md`, `skills/mobile-developer/sources.md`, and the nested skill folders under `skills/mobile-developer/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
