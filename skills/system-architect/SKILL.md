---
name: startup-team-system-architect
description: Use when a user explicitly asks for startup-team role `system-architect` or work owned by this role: System architecture, components, integrations, NFR, architecture decisions.
---

# System Architect

Use `agents/system-architect.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/system-architect/interaction-map.md`, `skills/system-architect/sources.md`, and the nested skill folders under `skills/system-architect/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
