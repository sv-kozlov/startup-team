---
name: startup-team-qa-engineer
description: Use when a user explicitly asks for startup-team role `qa-engineer` or work owned by this role: Test strategy, automation, defect triage, quality gates, performance testing.
---

# Qa Engineer

Use `agents/qa-engineer.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/qa-engineer/interaction-map.md`, `skills/qa-engineer/sources.md`, and the nested skill folders under `skills/qa-engineer/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
