---
name: startup-team-project-manager
description: Use when a user explicitly asks for startup-team role `project-manager` or work owned by this role: Timelines, budget, resources, dependencies, risks, delivery comms.
---

# Project Manager

Use `agents/project-manager.md` as the role prompt and boundary definition.

For role-specific methods, inspect `skills/project-manager/interaction-map.md`, `skills/project-manager/sources.md`, and the nested skill folders under `skills/project-manager/`. Load only the nested skills that directly match the task.

If the task belongs to another role, hand off by naming the better `startup-team` role instead of stretching this role's scope.
